# CSV-Combiner
A small tool to merge and combine CSV files into one CSV file.

1- Select the CSV files you want to combine.
2- Click with your mouse on the blue underlined field names you want to keep. (click again to remove!)
3- Press the Start Combining button to combine the selected CSV files, with the field names you picked earlier, into one CSV file.